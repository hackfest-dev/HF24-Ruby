import 'package:flutter/material.dart';

class NotifyVolunteer extends StatelessWidget {
  const NotifyVolunteer({Key? key, required String phoneNumber, required String password, required String hospital}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
